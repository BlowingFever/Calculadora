<Window x:Class="Calculadora.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:Calculadora.ViewModels"
        xmlns:models="clr-namespace:Calculadora.Models"
        Title="Calculadora"
        Width="460" Height="620"
        MinWidth="380" MinHeight="520"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen"
        Background="{DynamicResource WindowBackgroundBrush}">

    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>

    <Border Background="{DynamicResource CalcSurfaceBrush}"
            CornerRadius="{StaticResource CalcSurfaceCornerRadius}"
            Margin="16"
            Effect="{StaticResource CalcSurfaceShadow}">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ContentControl Grid.Row="0"
                            Content="{Binding CurrentView}"/>

            <Border Grid.Row="0"
                    VerticalAlignment="Bottom"
                    Height="1"
                    Background="{DynamicResource BorderBrush}"/>

            <!-- Bottom bar -->
            <Grid Grid.Row="1"
                  Background="{DynamicResource CalcSurfaceBrush}"
                  Margin="10,6,10,6">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0"
                            Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center">
                    <Button Content="Calculadora"
                            Style="{DynamicResource FuncButtonStyle}"
                            Width="120" Height="36"
                            Padding="16,0"
                            Command="{Binding ShowNormalCalculatorCommand}"/>
                    <Border Width="1"
                            Background="{DynamicResource BorderBrush}"
                            Margin="4,8"/>
                    <Button Content="Gráfica"
                            Style="{DynamicResource FuncButtonStyle}"
                            Width="80" Height="36"
                            Padding="16,0"
                            Command="{Binding ShowGraphCalculatorCommand}"/>
                </StackPanel>

                <!--
                    ThemeToggleButton: transparent background so that
                    it inherits the CalcSurfaceBrush from the parent Grid.
                    The ThemeToggleButtonStyle already has Background=Transparent.
                -->
                <models:ThemeToggleButton
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Margin="4,0,8,0"/>
            </Grid>
        </Grid>
    </Border>
</Window>